# MAG to MT Workflow

Builds a concatenated MAG reference, maps MT reads, quantifies CDS features, converts to TPM, and merges genome-resolved annotations. FASTA and GFF sequence IDs must remain identical and unique.
